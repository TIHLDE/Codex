---
title: "API Reference"
---

# {% $markdoc.frontmatter.title %}

```javascript
// Generate a bunch of code to see all syntax highlighting colors
const foo = () => {
  const bar = 'baz';
  return bar;
};

const baz = foo();

console.log(baz);
```
