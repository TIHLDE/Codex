import { nodes } from "@markdoc/markdoc";
import { ApiProps, CodeBlock } from "../../components";

export const api_props = {
  render: ApiProps,
  selfClosing: true,
  attributes: {
    props: {},
  },
};
